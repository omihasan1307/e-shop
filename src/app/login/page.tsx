import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login / E-Shop",
  description: "E-Shop",
};


const Login = () => {
  return (
    <div>
      <h1>sss</h1>
    </div>
  );
};

export default Login;
